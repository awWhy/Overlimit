New update broke Limit function, so it was removed from every further one since new Class is ~40 times faster and easier to work with (once get used to it). This branch is left just in case, before Limit function became broken
